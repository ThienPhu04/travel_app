import 'package:flutter/material.dart';

class FlightPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Chuyến bay"),
      ),
      body: Center(child: Text("This is the Flight Page")),
    );
  }
}
