import 'package:flutter/material.dart';

class TrainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vé tàu"),
      ),
      body: Center(child: Text("This is the Train Page")),
    );
  }
}
