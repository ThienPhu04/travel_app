import 'package:flutter/material.dart';

class ComboPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Combo tiết kiệm"),
      ),
      body: Center(child: Text("This is the Combo Page")),
    );
  }
}
