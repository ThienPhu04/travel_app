import 'package:flutter/material.dart';

class PartnerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Đối tác"),
      ),
      body: Center(child: Text("This is the Partner Page")),
    );
  }
}
